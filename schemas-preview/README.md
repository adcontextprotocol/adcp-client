# AdCP 3.2.0-rc.2 reporting-status preview

These schemas are vendored verbatim from `adcontextprotocol/adcp` main at commit
`388e78e63`. They temporarily define the `sync_reporting_status` contract until
AdCP 3.2.0-rc.2 is published and `npm run sync-schemas` can replace them.
