# AdCP 3.2.0-rc.2 reporting-status preview

These schemas are vendored verbatim from `adcontextprotocol/adcp` main at commit
`388e78e63`. This draft preview keeps `sync_reporting_status` isolated from the
SDK's rc.1 default pin; the dedicated rc.2 adoption removes this directory after
running `npm run sync-schemas -- 3.2.0-rc.2` and regenerating protocol types.
