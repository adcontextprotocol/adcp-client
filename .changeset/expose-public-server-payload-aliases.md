---
'@adcp/sdk': patch
---

Expose named server `*Payload` aliases from the root, types, and server barrels so adopters can annotate server-side adapter returns without importing wire `*Response` types.
