---
---

Release marker for the already-versioned SDK 14 prerelease. The major SDK
changeset is recorded in `pre.json` because `14.0.0-beta.0` has already been
applied; this empty marker keeps the pull-request changeset gate satisfied
without applying a second version bump.
