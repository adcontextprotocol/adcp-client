---
'@adcp/sdk': patch
---

Upgrade the bundled AdCP protocol schemas and generated SDK surfaces to 3.1.10.

This release adds Retina display catalog mappings for 2x-only and required 1x/2x rendition sets across the standard display sizes. It also updates the experimental Trusted Match types from provider-authored macro names to provider-local TMPX slots and adds the provider-to-router response and publisher mapping configuration schemas.
