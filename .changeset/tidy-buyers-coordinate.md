---
'@adcp/sdk': minor
---

Add a compact-first media-buy lifecycle coordinator that negotiates compact or established seller tools, normalizes compatibility provenance, preserves mutation retry keys, and rejects unrepresentable guarantees before dispatch unless the caller opts into an exact named loss.
