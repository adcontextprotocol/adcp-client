---
---

test(storyboard): cover ADCP-4009 signal-owned activation request shape.

Empty changeset because this only adds regression coverage for the storyboard
runner's existing request-builder behavior and does not change the published SDK
runtime API.
