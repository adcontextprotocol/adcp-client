---
'@adcp/sdk': minor
---

Add a validated RFC 8707 resource override to web and provider OAuth flows, preserve it during token refresh, support explicit removal, and add an opt-in Auth0 `audience` authorization parameter.
