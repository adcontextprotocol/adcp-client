---
"@adcp/sdk": patch
---

docs(skills): add concrete wire-shape pitfalls for discriminated unions to `build-signals-agent/SKILL.md` (`activation_key` `key_value` layout; `signal_ids` lookup element shape) and `build-creative-agent/SKILL.md` (cross-link to SHAPE-GOTCHAS.md union patterns). Skills ship in the npm package — patch bump.
