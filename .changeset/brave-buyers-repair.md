---
'@adcp/sdk': minor
---

Add a production buyer reporting worker with durable incremental repair, fenced multi-replica execution, polling, authenticated webhook hints, bounded concurrency, and graceful shutdown.
