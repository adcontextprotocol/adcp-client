---
'@adcp/sdk': patch
---

Backport strict legacy AdCP storyboard negotiation so compliance discovery can use the legacy `adcp_major_version` envelope without sending `adcp_version` to strict 3.0 sellers.
