---
---

Add a TypeScript reference seller storyboard harness for this repo's `examples/hello_seller_adapter_guaranteed.ts` server and restructure release interop around a reusable reference-seller matrix scaffold. The tagged compatibility matrix remains pinned to Python until a tagged TypeScript harness is available.
