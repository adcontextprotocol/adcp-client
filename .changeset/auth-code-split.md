---
"@adcp/sdk": minor
---

Export and document the AdCP 3.1 `AuthMissingError` / `AuthInvalidError` typed error classes while keeping deprecated `AuthRequiredError` wire-compatible with `AUTH_REQUIRED`.
