id: media_buy_seller/governance_conditions
version: "1.0.0"
title: "Seller attaches conditions when governance approves with conditions"
category: media_buy_seller
summary: "Verifies that the seller attaches governance conditions to the buy when governance approves with conditions."
track: media_buy
required_tools:
  - sync_governance
  - get_products
  - create_media_buy
  - comply_test_controller

narrative: |
  When governance approves a media buy with conditions, the seller must attach those
  conditions to the buy response. The buyer needs to see the conditions so they can
  comply with them (e.g., "weekly delivery reporting required for CTV buys").

  This scenario uses the test controller to force governance to return an
  approved_with_conditions outcome, then verifies the seller creates the buy
  and includes the governance conditions in the response.

agent:
  interaction_model: media_buy_seller
  capabilities:
    - sells_media
    - governance_aware
  examples:
    - "Any media buy seller with governance support"

caller:
  role: buyer_agent
  example: "Pinnacle Agency (buyer)"

prerequisites:
  description: |
    The seller must support sync_governance and comply_test_controller with
    force_governance_outcome scenario.
  test_kit: "test-kits/acme-outdoor.yaml"

phases:
  - id: setup
    title: "Account and governance setup"
    steps:
      - id: sync_accounts
        title: "Establish account"
        task: sync_accounts
        schema_ref: "account/sync-accounts-request.json"
        response_schema_ref: "account/sync-accounts-response.json"
        doc_ref: "/accounts/tasks/sync_accounts"
        stateful: true
        expected: |
          Return the account with account_id and status active.
        sample_request:
          accounts:
            - brand:
                domain: "acmeoutdoor.com"
              operator: "pinnacle-agency.com"
              billing: "operator"
              payment_terms: "net_30"
        validations:
          - check: response_schema
            description: "Response matches sync-accounts-response.json schema"
          - check: field_present
            path: "accounts[0].account_id"
            description: "Account has a platform-assigned ID"

      - id: sync_governance
        title: "Register governance agent"
        task: sync_governance
        schema_ref: "account/sync-governance-request.json"
        response_schema_ref: "account/sync-governance-response.json"
        doc_ref: "/accounts/tasks/sync_governance"
        stateful: true
        expected: |
          Acknowledge the governance agent registration.
        sample_request:
          accounts:
            - account:
                brand:
                  domain: "acmeoutdoor.com"
                operator: "pinnacle-agency.com"
              governance_agents:
                - url: "https://governance.pinnacle-agency.example"
                  authentication:
                    schemes: ["Bearer"]
                    credentials: "gov-token-xxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxx"
                  categories: ["budget_authority", "brand_policy"]
        validations:
          - check: response_schema
            description: "Response matches sync-governance-response.json schema"

  - id: force_and_buy
    title: "Force governance conditions and create buy"
    narrative: |
      Use the test controller to set governance to return approved_with_conditions
      for the next create_media_buy call, then verify the buy succeeds and conditions
      are attached.

    steps:
      - id: get_products_brief
        title: "Discover products"
        task: get_products
        schema_ref: "media-buy/get-products-request.json"
        response_schema_ref: "media-buy/get-products-response.json"
        doc_ref: "/media-buy/task-reference/get_products"
        comply_scenario: full_sales_flow
        stateful: false
        expected: |
          Return products matching the brief.
        sample_request:
          buying_mode: "brief"
          brief: "CTV and video inventory on outdoor lifestyle content. Q2 flight, $25K budget. Adults 25-54, US."
          brand:
            domain: "acmeoutdoor.com"
          account:
            brand:
              domain: "acmeoutdoor.com"
            operator: "pinnacle-agency.com"
        validations:
          - check: response_schema
            description: "Response matches get-products-response.json schema"
          - check: field_present
            path: "products"
            description: "Response contains products"

      - id: force_governance_conditions
        title: "Force governance to approve with conditions"
        task: comply_test_controller
        requires_tool: comply_test_controller
        stateful: true
        expected: |
          The test controller acknowledges the governance outcome override.
        sample_request:
          scenario: "force_governance_outcome"
          params:
            outcome: "approved_with_conditions"
            conditions:
              - "CTV buys require weekly delivery reporting"
              - "UGC placements require brand safety review before activation"
            governance_context: "gov-ctx-test-conditions"
        validations:
          - check: field_value
            path: "success"
            allowed_values: [true]
            description: "Controller accepts governance override"

      - id: create_media_buy_conditions
        title: "Create buy (governance approves with conditions)"
        task: create_media_buy
        schema_ref: "media-buy/create-media-buy-request.json"
        response_schema_ref: "media-buy/create-media-buy-response.json"
        doc_ref: "/media-buy/task-reference/create_media_buy"
        comply_scenario: create_media_buy
        stateful: true
        expected: |
          The buy succeeds with governance conditions attached:
          - media_buy_id: present
          - status: active or pending_creatives
          - governance_context: echoed from governance approval
          - conditions: governance conditions visible to the buyer
        sample_request:
          account:
            brand:
              domain: "acmeoutdoor.com"
            operator: "pinnacle-agency.com"
          brand:
            domain: "acmeoutdoor.com"
          start_time: "2026-04-01T00:00:00Z"
          end_time: "2026-06-30T23:59:59Z"
          packages:
            - product_id: "outdoor_ctv_q2"
              budget: 25000
              pricing_option_id: "cpm_standard"
        validations:
          - check: response_schema
            description: "Response matches create-media-buy-response.json schema"
