id: media_buy_seller/governance_approved
version: "1.0.0"
title: "Seller creates buy when governance approves"
category: media_buy_seller
summary: "Verifies that the seller creates a media buy when governance approves the transaction."
track: media_buy
required_tools:
  - sync_governance
  - get_products
  - create_media_buy

narrative: |
  This is a multi-agent test. The test harness sets up a permissive governance plan on
  a governance agent, then registers that agent with the seller. When the seller calls
  check_governance during create_media_buy, the governance agent approves because the
  buy falls within the plan's limits.

  The seller must call the governance agent and, on approval, confirm the buy normally.

  By default, the governance agent is the training agent at test-agent.adcontextprotocol.org.
  Override with --governance-agent-url to use a custom governance agent.

agent:
  interaction_model: media_buy_seller
  capabilities:
    - sells_media
    - governance_aware
  examples:
    - "Any media buy seller with governance support"

caller:
  role: buyer_agent
  example: "Pinnacle Agency (buyer)"

prerequisites:
  description: |
    A governance agent that supports sync_plans and check_governance. The training
    agent is used by default.
  test_kit: "test-kits/acme-outdoor.yaml"

phases:
  - id: governance_plan_setup
    title: "Set up permissive governance plan"
    narrative: |
      Create a governance plan on the governance agent with a high spending threshold.
      The subsequent buy will fall within these limits and should be approved.

    steps:
      - id: sync_plans
        title: "Create permissive governance plan"
        task: sync_plans
        schema_ref: "governance/sync-plans-request.json"
        response_schema_ref: "governance/sync-plans-response.json"
        doc_ref: "/governance/campaign/tasks/sync_plans"
        stateful: true
        expected: |
          The governance agent acknowledges the plan with a plan_id.
        sample_request:
          plans:
            - plan_name: "Acme Outdoor permissive governance"
              brand:
                domain: "acmeoutdoor.com"
              operator: "pinnacle-agency.com"
              authority_level: "agent_full"
              per_transaction_threshold: 100000
        validations:
          - check: response_schema
            description: "Response matches sync-plans-response.json schema"

  - id: seller_setup
    title: "Account and governance registration on seller"
    steps:
      - id: sync_accounts
        title: "Establish account with seller"
        task: sync_accounts
        schema_ref: "account/sync-accounts-request.json"
        response_schema_ref: "account/sync-accounts-response.json"
        doc_ref: "/accounts/tasks/sync_accounts"
        stateful: true
        expected: |
          Return the account with account_id and status active.
        sample_request:
          accounts:
            - brand:
                domain: "acmeoutdoor.com"
              operator: "pinnacle-agency.com"
              billing: "operator"
              payment_terms: "net_30"
        validations:
          - check: response_schema
            description: "Response matches sync-accounts-response.json schema"
          - check: field_present
            path: "accounts[0].account_id"
            description: "Account has a platform-assigned ID"

      - id: sync_governance
        title: "Register governance agent with seller"
        narrative: |
          Register the governance agent URL with the seller. The seller will call
          check_governance on this URL during create_media_buy.
        task: sync_governance
        schema_ref: "account/sync-governance-request.json"
        response_schema_ref: "account/sync-governance-response.json"
        doc_ref: "/accounts/tasks/sync_governance"
        stateful: true
        expected: |
          Acknowledge the governance agent registration.
        sample_request:
          accounts:
            - account:
                brand:
                  domain: "acmeoutdoor.com"
                operator: "pinnacle-agency.com"
              governance_agents:
                - url: "$context.governance_agent_url"
                  categories: ["budget_authority", "brand_policy"]
        validations:
          - check: response_schema
            description: "Response matches sync-governance-response.json schema"

  - id: buy_approved
    title: "Create buy within governance limits"
    narrative: |
      Send a brief and create a buy within the plan's spending threshold ($25K vs $100K limit).
      The governance agent approves. The seller confirms the buy.

    steps:
      - id: get_products_brief
        title: "Discover products"
        task: get_products
        schema_ref: "media-buy/get-products-request.json"
        response_schema_ref: "media-buy/get-products-response.json"
        doc_ref: "/media-buy/task-reference/get_products"
        comply_scenario: full_sales_flow
        stateful: false
        expected: |
          Return products matching the brief.
        sample_request:
          buying_mode: "brief"
          brief: "Display and video inventory on outdoor lifestyle content. Q2 flight, $25K budget. Adults 25-54, US."
          brand:
            domain: "acmeoutdoor.com"
          account:
            brand:
              domain: "acmeoutdoor.com"
            operator: "pinnacle-agency.com"
        validations:
          - check: response_schema
            description: "Response matches get-products-response.json schema"
          - check: field_present
            path: "products"
            description: "Response contains products"

      - id: create_media_buy
        title: "Create buy (governance approves)"
        task: create_media_buy
        schema_ref: "media-buy/create-media-buy-request.json"
        response_schema_ref: "media-buy/create-media-buy-response.json"
        doc_ref: "/media-buy/task-reference/create_media_buy"
        comply_scenario: create_media_buy
        stateful: true
        expected: |
          The buy succeeds — governance approved because the $25K buy is within
          the plan's $100K per-transaction threshold.
        sample_request:
          account:
            brand:
              domain: "acmeoutdoor.com"
            operator: "pinnacle-agency.com"
          brand:
            domain: "acmeoutdoor.com"
          start_time: "2026-04-01T00:00:00Z"
          end_time: "2026-06-30T23:59:59Z"
          packages:
            - product_id: "outdoor_display_q2"
              budget: 15000
              pricing_option_id: "cpm_standard"
            - product_id: "outdoor_video_q2"
              budget: 10000
              pricing_option_id: "cpm_standard"
        validations:
          - check: response_schema
            description: "Response matches create-media-buy-response.json schema"
