// AdCP SDK — client surface
//
// Buyer-side: instantiate clients to call AdCP agents over MCP/A2A,
// orchestrate multi-agent flows, and inspect responses.
//
// `import { AdcpClient } from '@adcp/sdk'` and
// `import { AdcpClient } from '@adcp/sdk/client'` resolve to the same surface.
// The subpath exists for symmetry with `/server` and `/compliance`.

export * from '../index';
