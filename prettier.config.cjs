module.exports = {
  // Only format files that opt-in via a pragma at the top
  requirePragma: true,

  // Match repo style inferred from AsyncHandler.ts
  semi: true,
  singleQuote: true,
  trailingComma: 'es5',
  bracketSpacing: true,
  arrowParens: 'always',
  printWidth: 100,
  tabWidth: 2,
  useTabs: false,
};
